const firstYearCompleted = true;
const yearsCompleted = 1;

yearsCompleted == firstYearCompleted;
yearsCompleted === firstYearCompleted;

// double equals are for the checking the values. if we use triple equals,
// both value and type should be exact same.