function canPlay() {
  let sport = " Football";

  if (true) {
    var personName = "Cosimo";
  }

  console.log(personName + sport);
}

canPlay();
//Inside of if block, "let" can't go out of the block. We should use
// var instead.