let hello = Number('Ciao');     // number
let age = Boolean(18);       // boolean
let isGraduated = false.toString();     // string

console.log(hello);
console.log(age);
console.log(isGraduated);

// Hello output is NaN (not a number) because it is not convertable to
// a numeric type.